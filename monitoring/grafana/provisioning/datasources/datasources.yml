# Grafana data source: the audit log store the intrusion-detection rules query.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki   # fixed uid so provisioned alert rules can reference it
    access: proxy
    url: http://loki:3100
    isDefault: true
